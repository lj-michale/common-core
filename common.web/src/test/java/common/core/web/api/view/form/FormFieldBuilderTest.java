package common.core.web.api.view.form;

import org.junit.Test;

public class FormFieldBuilderTest {

	@Test
	public void buildHiddenFormFields() {

		// FormField formField = new FormField();
		// formField.setValue("v1");
		// formField.setName("n1");
		// Assert.assertEquals(5,
		// FormFieldBuilder.buildHiddenFormFields(formField).size());
		// Assert.assertEquals(4, FormFieldBuilder.buildHiddenFormFields(formField, new
		// String[] { "name" }).size());
	}
}
